{
    "language": "py",
    "mini_language_version": "3.6",
    "author": "hsz",
    "description": "python自带的xmlrpc的客户端和服务器模板",
    "template_type": "c&s",
    "requires": [
        "schema_entry>=0.1.5",
        "pyloggerhelper>=0.0.2"
    ],
    "test_requires": [
        "coverage >= 5.5",
        "mypy >= 0.800",
        "autopep8 >= 1.5.6",
        "pylint >= 2.8.0",
        "pydocstyle>=6.0.0"
    ],
    "test": {
        "source": "cp_python_test//simplerpc_tests",
        "default_path": "tests"
    },
    "components": {
        "app": {
            "source": "app",
            "description": "xmlrpc的服务应用,默认部署在`app`目录",
            "default_path": "app"
        },
        "cli": {
            "source": "cli.py.jinja",
            "description": "xmlrpc的客户端,默认部署在`项目名称的全小写_cli.py`文件",
            "default_path": "{{ project_name | lower }}_cli.py"
        },
        "dockerfile": {
            "source": "cp_dockerfiles//python_singal_dockerfile",
            "description": "xmlrpc的客户端,默认部署在`项目名称的全小写_cli.py`文件",
            "default_path": "dockerfile"
        }
    },
    "template_keys": {
        "app_name": {
            "description": "服务应用名,默认为项目名称",
            "default": "{{ project_name }}"
        },
        "app_name_camelcase": {
            "description": "服务应用名的驼峰形式,默认为项目名称",
            "default": "{{ project_name | replace(\"_\", \" \") | replace(\"-\", \" \") | title | replace(\" \", \"\") }}"
        }
    }
}